<UserControl x:Class="BiblioGest.Views.LivreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BiblioGest.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Margin="10">
        <TextBlock Text="Gestion des Livres" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Formulaire -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="100" Margin="5" Text="{Binding NewLivre.ISBN}" ToolTip="ISBN"/>
            <TextBox Width="150" Margin="5" Text="{Binding NewLivre.Titre}" ToolTip="Titre"/>
            <TextBox Width="100" Margin="5" Text="{Binding NewLivre.Auteur}" ToolTip="Auteur"/>
            <TextBox Width="100" Margin="5" Text="{Binding NewLivre.Editeur}" ToolTip="Editeur"/>
            <TextBox Width="50" Margin="5" Text="{Binding NewLivre.AnneePublication}" ToolTip="Année"/>
            <TextBox Width="100" Margin="5" Text="{Binding NewLivre.Categorie}" ToolTip="Catégorie"/>
            <TextBox Width="50" Margin="5" Text="{Binding NewLivre.NombreExemplaires}" ToolTip="Stock"/>
        </StackPanel>

        <!-- Boutons -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Ajouter" Command="{Binding AddLivreCommand}" Width="100" Margin="5"/>
            <Button Content="Supprimer" Command="{Binding DeleteLivreCommand}" Width="100" Margin="5"/>
        </StackPanel>

        <!-- Liste -->
        <DataGrid ItemsSource="{Binding Livres}" SelectedItem="{Binding SelectedLivre}" AutoGenerateColumns="False" Height="300">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" />
                <DataGridTextColumn Header="Titre" Binding="{Binding Titre}" />
                <DataGridTextColumn Header="Auteur" Binding="{Binding Auteur}" />
                <DataGridTextColumn Header="Editeur" Binding="{Binding Editeur}" />
                <DataGridTextColumn Header="Année" Binding="{Binding AnneePublication}" />
                <DataGridTextColumn Header="Catégorie" Binding="{Binding Categorie}" />
                <DataGridTextColumn Header="Stock" Binding="{Binding NombreExemplaires}" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
