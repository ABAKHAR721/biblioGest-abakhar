<UserControl x:Class="BiblioGest.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:BiblioGest.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid Background="#FAFAFA">
        <Border Padding="20" Background="White" CornerRadius="10" Width="350" Height="250" HorizontalAlignment="Center" VerticalAlignment="Center" >
            <StackPanel>
                <TextBlock Text="Connexion" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                <TextBlock Text="Identifiant"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <TextBlock Text="Mot de passe"/>
                <PasswordBox x:Name="PwdBox" Margin="0,0,0,10" PasswordChanged="PwdBox_PasswordChanged"/>

                <CheckBox Content="Se souvenir de moi" IsChecked="{Binding RememberMe}" Margin="0,0,0,10"/>

                <Button Content="Se connecter" Command="{Binding LoginCommand}" Height="35" Background="#4CAF50" Foreground="White"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
